Flask==3.0.0
Werkzeug==3.0.1
pypdf==3.17.4
img2pdf==0.5.1
Pillow==10.2.0
python-docx==1.1.0
python-pptx==0.6.23
openpyxl==3.1.2
xhtml2pdf==0.2.14
reportlab==4.0.9
pandas==2.2.0
# We use pdf2docx for conversion, but we must pin a compatible version or risk size limits
# If pdf2docx fails due to size (OpenCV), remove it.
pdf2docx==0.5.8
opencv-python-headless==4.9.0.80